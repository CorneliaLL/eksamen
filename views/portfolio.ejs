<html lang="en">
<%- include ('./partials/head.ejs') %>

<!-- Link to dashboard stylesheet -->
<link rel="stylesheet" href="/dashboard.css">

<body>

    <!-- Main content -->
    <!-- Navigation bar -->
    <%- include ('./partials/nav.ejs') %>

    <div class="container">
        <!-- Title section -->
        <div class="containerTitle">
            <h1>Dashboard</h1> <!-- Gets username or heading from database -->
        </div>

        <!-- Summary value boxes -->
        <div class="summaryRow">
            <div class="summaryBox">
                <div class="summaryHeader">Total Value</div>
                <div class="summaryAmount">
                    <%= typeof totalAcquisitionPrice !== 'undefined' ? totalAcquisitionPrice.toFixed(2) : '0.00' %>
                </div>
            </div>
            <div class="summaryBox">
                <div class="summaryHeader">Total Realized Value</div>
                <div class="summaryAmount">
                    <%= typeof totalRealizedValue !== 'undefined' ? totalRealizedValue.toFixed(2) : '0.00' %>
                </div>
            </div>
            <div class="summaryBox">
                <div class="summaryHeader">Total Unrealized Gain</div>
                <div class="summaryAmount">
                    <%= typeof totalUnrealizedGain !== 'undefined' ? totalUnrealizedGain.toFixed(2) : '0.00' %>
                </div>
            </div>
        </div>

        <div class="valueBox">
            <h2>Total value overview</h2>
            <!-- This section can hold more detailed charts or summary if needed -->
        </div>

<<<<<<< Updated upstream
      <!-- Holdings table -->
      <div class="dashboardBox3">
        <h2>Holdings in Portfolio</h2>
        <div class="tableContainer">
          <table>
            <thead>
              <tr>
                <th>Ticker</th>
                <th>Quantity</th>
                <th>24h Change</th>
                <th>Price</th>
                <th>GAK</th>
              </tr>
            </thead>
            <tbody>
              <% holdings.forEach(stock => { %>
                <tr>
                  <td><%= stock.ticker %></td>
                  <td><%= stock.quantity %> pcs</td>
                  <td class="<%= stock.dailyChange >= 0 ? 'positive' : 'negative' %>">
                    <%= stock.dailyChange ? stock.dailyChange.toFixed(2) + '%' : 'N/A' %>
                  </td>
                  <td><%= stock.currentPrice ? stock.currentPrice.toFixed(2) + ' ' + account.currency : 'N/A' %></td>
                  <td><%= stock.gak ? stock.gak.toFixed(2) : 'N/A' %></td>
                </tr>
              <% }) %>
            </tbody>
          </table>
        </div>
      </div>
=======
        <!-- Optional: Line chart section for future data visualization -->
        <!-- 
        <section style="margin-top: 30px; padding: 20px;">
            <div style="background-color: #1e1e1e; padding: 20px; border-radius: 15px; max-width: 1000px; margin: auto;">
                <h2 style="text-align: center;">Portfolio Value Over Time</h2>
                <canvas id="totalPortfolioChart" width="900" height="400"></canvas>
            </div>
        </section>
        <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
        <script src="/js/dashboardChart.js"></script>
        -->
>>>>>>> Stashed changes

    </div>

</body>

</html>
